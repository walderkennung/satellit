[project]
name = "satellit_sam"
version = "0.1.0"
description = "Satellite imagery processing with SAM"
authors = [{ name = "Mose Schmiedel", email = "mose@schmiednet.de" }]
requires-python = ">=3.10"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/satellit_sam"]
